// This component has been removed for security reasons.
// Role switching is not allowed in production.
// Users get their role based on authentication only.

export default function RoleSwitcher() {
  return null;
}
